export type User = {
    id: string;
    name: string;
    email: string;
    password: string;
    role: string;
}
export type Record = {
    id: string;
    name: string;
    city: string;
    timezone: string;
    diff_gmt: string;
    creator: string;
}